#include "feather.h"
#include "internal.h"

// Temporary stubs for usage registration functions that haven't been implemented yet
// Already implemented in builtin files: break, catch, continue, error, expr, for, foreach, if,
// incr, info, lassign, lindex, linsert, llength, lmap, mathfunc, namespace, proc, rename, return, set, switch, tailcall, uplevel, upvar, variable, while, global, apply, throw, try, list, trace, lrange, lappend, lset, lreverse, lreplace, lrepeat, lsort, lsearch, string, split, join, dict, concat, append, unset, format, scan, subst, eval

// All usage functions are now implemented!
