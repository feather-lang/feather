/* Code generated by cmd/cgo; DO NOT EDIT. */

/* package github.com/feather-lang/feather/cmd/libfeather */


#line 1 "cgo-builtin-export-prolog"

#include <stddef.h>

#ifndef GO_CGO_EXPORT_PROLOGUE_H
#define GO_CGO_EXPORT_PROLOGUE_H

#ifndef GO_CGO_GOSTRING_TYPEDEF
typedef struct { const char *p; ptrdiff_t n; } _GoString_;
extern size_t _GoStringLen(_GoString_ s);
extern const char *_GoStringPtr(_GoString_ s);
#endif

#endif

/* Start of preamble from import "C" comments.  */


#line 7 "exports.go"

#include <stdlib.h>
#include <stdint.h>
#include <string.h>

// Handle types (size_t to match Go's export)
typedef size_t FeatherInterp;
typedef size_t FeatherObj;

// Callback type for custom commands registered from C.
// Commands receive handles and return result/error as handles.
// Return 0 for success, non-zero for error.
typedef int (*FeatherCmd)(void *data, FeatherInterp interp,
                          size_t argc, FeatherObj *argv,
                          FeatherObj *result, FeatherObj *err);

// Wrapper function to call C callback from Go
static inline int callCCallback(FeatherCmd cb, void *data,
                                FeatherInterp interp, size_t argc, FeatherObj *argv,
                                FeatherObj *result, FeatherObj *err) {
    return cb(data, interp, argc, argv, result, err);
}

// Foreign type callbacks
typedef void* (*FeatherForeignNewFunc)(void *userData);
typedef int (*FeatherForeignInvokeFunc)(void *instance, FeatherInterp interp,
                                         const char *method, size_t argc, FeatherObj *argv,
                                         FeatherObj *result, FeatherObj *err);
typedef void (*FeatherForeignDestroyFunc)(void *instance);

// Wrapper functions for foreign type callbacks
static inline void* callForeignNew(FeatherForeignNewFunc fn, void *userData) {
    return fn(userData);
}

static inline int callForeignInvoke(FeatherForeignInvokeFunc fn, void *instance,
                                    FeatherInterp interp, const char *method,
                                    size_t argc, FeatherObj *argv,
                                    FeatherObj *result, FeatherObj *err) {
    return fn(instance, interp, method, argc, argv, result, err);
}

static inline void callForeignDestroy(FeatherForeignDestroyFunc fn, void *instance) {
    fn(instance);
}

#line 1 "cgo-generated-wrapper"


/* End of preamble from import "C" comments.  */


/* Start of boilerplate cgo prologue.  */
#line 1 "cgo-gcc-export-header-prolog"

#ifndef GO_CGO_PROLOGUE_H
#define GO_CGO_PROLOGUE_H

typedef signed char GoInt8;
typedef unsigned char GoUint8;
typedef short GoInt16;
typedef unsigned short GoUint16;
typedef int GoInt32;
typedef unsigned int GoUint32;
typedef long long GoInt64;
typedef unsigned long long GoUint64;
typedef GoInt64 GoInt;
typedef GoUint64 GoUint;
typedef size_t GoUintptr;
typedef float GoFloat32;
typedef double GoFloat64;
#ifdef _MSC_VER
#if !defined(__cplusplus) || _MSVC_LANG <= 201402L
#include <complex.h>
typedef _Fcomplex GoComplex64;
typedef _Dcomplex GoComplex128;
#else
#include <complex>
typedef std::complex<float> GoComplex64;
typedef std::complex<double> GoComplex128;
#endif
#else
typedef float _Complex GoComplex64;
typedef double _Complex GoComplex128;
#endif

/*
  static assertion to make sure the file is being used on architecture
  at least with matching size of GoInt.
*/
typedef char _check_for_64_bit_pointer_matching_GoInt[sizeof(void*)==64/8 ? 1:-1];

#ifndef GO_CGO_GOSTRING_TYPEDEF
typedef _GoString_ GoString;
#endif
typedef void *GoMap;
typedef void *GoChan;
typedef struct { void *t; void *v; } GoInterface;
typedef struct { void *data; GoInt len; GoInt cap; } GoSlice;

#endif

/* End of boilerplate cgo prologue.  */

#ifdef __cplusplus
extern "C" {
#endif

extern size_t FeatherNew(void);
extern void FeatherClose(size_t interp);
extern int FeatherParse(size_t interp, char* script, size_t length);
extern int FeatherParseInfo(size_t interp, char* script, size_t length, size_t* result, size_t* errorObj);
extern int FeatherEval(size_t interp, char* script, size_t length, size_t* result);
extern int FeatherCall(size_t interp, size_t argc, size_t* argv, size_t* result);
extern size_t FeatherString(size_t interp, char* s, size_t length);
extern size_t FeatherInt(size_t interp, int64_t val);
extern size_t FeatherDouble(size_t interp, double val);
extern size_t FeatherList(size_t interp, size_t argc, size_t* argv);
extern size_t FeatherDict(size_t interp);
extern int64_t FeatherAsInt(size_t interp, size_t obj, int64_t def);
extern double FeatherAsDouble(size_t interp, size_t obj, double def);
extern int FeatherAsBool(size_t interp, size_t obj, int def);
extern size_t FeatherLen(size_t interp, size_t obj);
extern int FeatherByteAt(size_t interp, size_t obj, size_t index);
extern int FeatherEq(size_t interp, size_t a, size_t b);
extern int FeatherCmp(size_t interp, size_t a, size_t b);
extern size_t FeatherCopy(size_t interp, size_t obj, char* buf, size_t length);
extern size_t FeatherListLen(size_t interp, size_t list);
extern size_t FeatherListAt(size_t interp, size_t list, size_t index);
extern size_t FeatherListPush(size_t interp, size_t list, size_t item);
extern size_t FeatherDictLen(size_t interp, size_t dict);
extern size_t FeatherDictGet(size_t interp, size_t dict, size_t key);
extern size_t FeatherDictSet(size_t interp, size_t dict, size_t key, size_t value);
extern int FeatherDictHas(size_t interp, size_t dict, size_t key);
extern size_t FeatherDictKeys(size_t interp, size_t dict);
extern void FeatherSetVar(size_t interp, char* name, size_t val);
extern size_t FeatherGetVar(size_t interp, char* name);
extern void FeatherRegister(size_t interp, char* name, FeatherCmd fn, void* data);
extern int FeatherRegisterForeignMethod(size_t interp, char* typeName, char* methodName);
extern int FeatherRegisterForeign(size_t interp, char* typeName, FeatherForeignNewFunc newFn, FeatherForeignInvokeFunc invokeFn, FeatherForeignDestroyFunc destroyFn, void* userData);

#ifdef __cplusplus
}
#endif
